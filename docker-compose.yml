version: '3'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    networks:
      - flask_network

  swagger:
    image: swaggerapi/swagger-ui
    ports:
      - "8080:8080"
    environment:
      - SWAGGER_JSON=/swagger.json
    volumes:
      - ./swagger.json:/swagger.json:ro
    networks:
      - flask_network

networks:
  flask_network: